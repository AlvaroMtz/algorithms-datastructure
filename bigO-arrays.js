let names = ["Michael", "Melisa", "Andrea"]
let values = [true, {}, [], 2, "awesome"]

//BigO of Arrays (if you need order)
// Insertion -> It depends
// Removal -> It depends
// Searching -> O(n)
// Access -> O(1)
